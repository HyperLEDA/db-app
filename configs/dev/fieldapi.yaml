server:
  host: 0.0.0.0
  port: 8082
  allowed_origins:
    - "*"

tracing:
  endpoint: localhost:4317
  enabled: true

datasets:
  data_dir: downloads/fieldapi
  enabled:
    - id: sfd
      provider: sfd
      name: SFD
      version: "1998"
