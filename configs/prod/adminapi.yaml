server:
  host: 0.0.0.0
  port: 8080
  path_prefix: /admin/api

auth_enabled: false

storage:
  endpoint: hyperledadb
  port: 5432
  dbname: hyperleda
  user: hyperleda
  password: fake

queue:
  endpoint: redis
  port: 6379
  queue_name: default

clients:
  enabled: true
  ads_token: fake
