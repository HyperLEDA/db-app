deploy:
	set -a && . .env.local && set +a && ./scripts/deploy.sh

restart:
	set -a && . .env.local && set +a && ./scripts/restart.sh

deploy-test-db:
	set -a && . .env.local && set +a && ./scripts/deploy-test-db.sh
